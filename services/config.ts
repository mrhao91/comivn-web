
// Set this to true to use local storage (Mock Data) for testing/preview.
// Set this to false ONLY when you have deployed the Backend (Node.js) to your hosting.
export const USE_MOCK_DATA = true; 

// Base URL for the API.
// If USE_MOCK_DATA is true, this is ignored.
export const API_BASE_URL = '/api';
